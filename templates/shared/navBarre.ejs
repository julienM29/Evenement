<nav>
    <div class="navbar navbar-light navbar-expand-md px-2 shadow position-fixed w-100 z-2"
        style="background-color: rgb(255, 255, 255); top: 0; left: 0;">
        <div style="width: 15%;">
            <a href="/" class="navbar-brand d-flex justify-content-center">
                <img src="/images/Event1.png" alt="" width="235px" height="65px" />
            </a>
        </div>
        <div class="d-flex justify-content-end align-items-center" style="width: 85%;" id="menu">
            <div class="w-75 d-flex justify-content-around">
                <div class="w-50 d-flex justify-content-center align-items-center">
                    <% if (locals.user) { %>
                        <div class="" id="ajout">
                            <a href="/creation" class="underline fs-3 ms-2 align-items-center"><i
                                    class="fa-solid fa-circle-plus me-1"></i></i>Ajouter un
                                évènement</a>
                        </div>
                        <% } else { %>
                            <li class="fw-bolder px-3 d-flex align-items-center">
                                <a href="/register" class="underline fs-3 ms-2 align-items-center"><i
                                        class="fa-solid fa-circle-plus me-1"></i></i>Création de compte</a>
                            </li>
                            <li class="ms-5 fw-bolder  px-3 d-flex align-items-center">
                                <a href="/login" class="underline fs-3 ms-2 align-items-center"><i
                                        class="fa-solid fa-right-to-bracket me-1"></i>Connexion</a>
                            </li>
                            <% } %>
                </div>
                <div class="w-50 d-flex justify-content-around align-items-center">
                    <% if (locals.user) { %>
                        <div>
                            <% if (nbNotifMessageNonLus) { %>
                                <a href="/messagerie/<%= user.id %>"
                                    class="lienNotificationMessage text-body aling-items-center">
                                    <i class="fas fa-message fa-2x"></i>
                                    <% if (nbNotifMessageNonLus> 9) { %>
                                        <span class="badge rounded-pill badge-notification bg-danger">9+</span>
                                        <% } else if (nbNotifMessageNonLus> 0) { %>
                                            <span class="badge rounded-pill badge-notification bg-danger">
                                                <%= nbNotifMessageNonLus %>
                                            </span>
                                            <% } %>
                                </a>
                                <% } else { %>
                                    <a href="/messagerie/<%= user.id %>" class="lienNotificationMessage text-body">
                                        <i class="fas fa-message fa-2x"></i>
                                    </a>
                                    <% } %>
                        </div>
                        <div>
                            <% if (nbNotifEventNonLus) { %>
                                <a href="/notification/<%= user.id %>"
                                    class="lienNotificationMessage text-body aling-items-center">
                                    <i class="fas fa-envelope fa-2x"></i>
                                    <% if (nbNotifEventNonLus.nb_notifs> 9) { %>
                                        <span class="badge rounded-pill badge-notification bg-danger">9+</span>
                                        <% } else if (nbNotifEventNonLus.nb_notifs> 0) { %>
                                            <span class="badge rounded-pill badge-notification bg-danger">
                                                <%= nbNotifEventNonLus.nb_notifs %>
                                            </span>
                                            <% } %>
                                </a>
                                <% } else { %>
                                    <a href="" class="lienNotificationMessage text-body">
                                        <i class="fas fa-envelope fa-2x"></i>
                                    </a>
                                    <% } %>
                        </div>
                        <div>
                            <li class="fw-bolder navButton rounded-pill d-flex align-items-center">
                                <a href="/profil/<%= user.id %>" data-bs-toggle="tooltip" data-bs-placement="bottom"
                                    title="Voir votre profil">
                                    <img class="rounded-circle border border-2 border-secondary p-1"
                                        src="/images/<%= user.photo %>" width="65" height="65">
                                </a>
                            </li>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>